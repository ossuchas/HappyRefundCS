import { Tf01docstatusPipe } from './tf01docstatus.pipe';

describe('Tf01docstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new Tf01docstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
